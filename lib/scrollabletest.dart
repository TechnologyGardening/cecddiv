import 'package:flutter/material.dart';
import 'package:wheel_picker/wheel_picker.dart';

class ScrollPickertest extends StatefulWidget {
  const ScrollPickertest({super.key});

  @override
  State<ScrollPickertest> createState() => _ScrollPickertestState();
}

class _ScrollPickertestState extends State<ScrollPickertest> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
